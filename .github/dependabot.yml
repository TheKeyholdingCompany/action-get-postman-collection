version: 2
updates:
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      docker-deps:
        patterns: ["*"]
        update-types: ["patch", "minor", "major"]
    cooldown:
      default-days: 3
      semver-major-days: 14
